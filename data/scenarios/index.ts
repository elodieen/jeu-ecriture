import type { Scenario } from './types';
import { derniereSaison } from './derniere-saison';

// Ajouter un nouveau scénario ici — c'est le seul endroit à modifier.
export const scenarios: Scenario[] = [
  derniereSaison,
];
